# Implemented foundation

OAuth token renewal, active broadcast discovery, authenticated chat reads, normalized messages, mapped-stream forwarding, continuation tokens, and provider polling cadence are implemented on this branch.
