# Current limitation

The branch can perform authenticated incremental ingestion, but continuous unattended delivery still needs to run from the long-lived worker. The worker upgrade is the next production step.
