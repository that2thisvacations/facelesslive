# YouTube live-chat ingestion

Adds refreshable YouTube OAuth access, active broadcast/live-chat discovery, authenticated incremental chat reads, normalized viewer events, and mapped forwarding into FacelessLive's existing live connector. Continuous low-latency server streaming remains a follow-up worker change.
