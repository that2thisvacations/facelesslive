# Milestone

The first authenticated end-to-end YouTube chat batch can now travel from the connected channel into the existing FacelessLive live connector pipeline when the broadcast is mapped to an active stream job.
