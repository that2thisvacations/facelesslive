# Authentication

YouTube discovery, chat reads, and forwarding require the signed-in FacelessLive user's Supabase bearer session. Arbitrary owner IDs are not accepted by the updated authenticated routes.
