# Flow

Connected YouTube account -> token refresh -> active broadcast discovery -> live chat ID -> normalized viewer message -> mapped FacelessLive stream -> existing AI response and moderation pipeline.
