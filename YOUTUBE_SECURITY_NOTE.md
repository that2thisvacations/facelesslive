# Security note

The new discovery and chat routes are implementation scaffolds. Do not deploy them for direct browser use until caller-supplied owner context is replaced by validated FacelessLive authentication or a trusted server-worker identity.
