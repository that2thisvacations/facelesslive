# Token refresh

Expired or near-expiry YouTube access tokens are renewed server-side using the encrypted stored refresh token, and the updated encrypted token set is persisted back to the provider connection.
